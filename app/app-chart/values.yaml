replicaCount: 2

fullnameOverride: "app"

image:
  repository: luwao/refactor-app

service:
  type: NodePort
  port: 8000

postgresql:
  enabled: true
  name: postgres
  postgresqlUsername: myuser
  postgresqlPassword: passwd
  postgresqlDatabase: myapp
