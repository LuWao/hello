apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "order-chart.fullname" . }}
  labels:
    {{- include "order-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "order-chart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "order-chart.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: "{{ .Chart.Name }}-worker"
          image: "{{ .Values.image.repository }}:{{ .Chart.AppVersion }}"
          command: ["bash", "entrypoint.sh", "celery-worker"]
          env:
            - name: ORDER_DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: ORDER_DATABASE_URI
            - name: ORDER_BROKER_URI
              value: "{{ .Values.brokerURI }}/0"
            - name: BROKER_URI
              value: "{{ .Values.brokerURI }}/4"
        - name: "{{ .Chart.Name }}-api"
          image: "{{ .Values.image.repository }}:{{ .Chart.AppVersion }}"
          command: ["bash", "entrypoint.sh", "api"]
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: ORDER_DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: ORDER_DATABASE_URI
            - name: ORDER_BROKER_URI
              value: "{{ .Values.brokerURI }}/0"
            - name: BROKER_URI
              value: "{{ .Values.brokerURI }}/4"
